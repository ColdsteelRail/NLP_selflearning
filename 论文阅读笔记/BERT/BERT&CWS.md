论文地址：https://arxiv.org/abs/1909.09292
介绍：
    CWS：dividing text into words, is a key preprocessing step for Chinese language understanding
GRNN(门控递归神经网络),LSTM(长短期记忆网络) and CNN(卷积神经网络)被应用于在句子的分段中构建一致性模型，但它们都需要固定的上写问窗口宽度。
但是GCNN(门控组合神经网络)克服了这一缺点